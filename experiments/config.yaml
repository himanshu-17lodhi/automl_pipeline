experiment_name: "customer_churn_v1"
target_column: "churn"
test_size: 0.2
timeout_seconds: 60 
models:
  random_forest:
    active: true
    param_grid:
      n_estimators: [50, 100]
      max_depth: [5, 10]
  xgboost:
    active: true
    param_grid:
      learning_rate: [0.01, 0.1]
      n_estimators: [50, 100]